#!/bin/bash
#Backing up script

if [ $# != 1 ]; then
  echo "Usage: $0 <file_to_backup>"
  exit 1
fi

file=$1
backup_dir=~/backup

# Create backup director if it doesn't exit
mkdir -p $backup_dir

#Copy the file to the backup directory
cp $file $backup_dir

echo "Backup of $file completed!"
