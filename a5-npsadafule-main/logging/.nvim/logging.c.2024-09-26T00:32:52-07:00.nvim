#include <stdio.h>

int main() {
  int size = 5;
  int numbers[size];
  int sum = 0;

  for(int i=0; i <= size; i++){
    numbers[i] = i;
    sum += numbers[i];
  }

  printf("The sum is: %d\n", sum);

  return 0;
}
